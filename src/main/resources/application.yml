
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fullstackapp
    password: Fiesta.2018
    username: root
    jpa:
      database-platform: org.hibernate.dialect.MySQL5Dialect
      generate-ddl: true
      show-sql: true
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          globally_quoted_identifiers: true
          dialect: org.hibernate.dialect.MySQL5Dialect
          format_sql: true
  sql:
    init:
      mode: never


